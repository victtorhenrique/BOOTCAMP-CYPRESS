{
  "name": "bootcamp-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:run" : "npx cypress run",
    "allure:tests": "npx cypress run && npx allure serve allure-results && npx allure generate allure-results",
    "allure:report": "npx cypress run && npx allure generate --clean allure-results"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.29.0",
    "allure-cypress": "^3.0.0-beta.5",
    "cypress": "^13.13.0"
  }
}
